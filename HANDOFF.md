# GYUTRON gyutron.com — Engineering Handoff (single source of truth)

> Consolidated handoff for the agent/engineer taking over. **This supersedes the older, partly-stale rules in `AGENTS.md` where they conflict** (see §10). Last updated 2026-06-06. Current live HEAD: use `git rev-parse origin/main` after `git fetch`.
> Talk to the user in **Chinese**; keep code / i18n keys / brand names verbatim.
> 🏗️ **2026-06-06:** an **additive engineering foundation** was added — repo-root **`docs/`** + `astro/src/{config,types,data}` + `astro/src/lib/{api,forms,crm,cms,agent,logger}` (mock/placeholder) + `astro/.env.example`. See **§5b** and `docs/`.
> 🧭 **New maintainer / employee / agent: START at `docs/MAINTENANCE.md`** (a task→doc index for every common change) and read **`docs/SAFETY_CHECKLIST.md`** (what breaks the site). Other guides: ARCHITECTURE, COMPONENTS, CONTENT_GUIDE, ROUTES, I18N, SEO, ACCESSIBILITY, PERFORMANCE, FUTURE_INTEGRATIONS, SOLUTIONS_GUIDE, DEPLOYMENT, TROUBLESHOOTING.
> 🛰️ **2026-06-10 (Claude): Phase-1 BACKEND + Data API landed** — repo-root **`src/` worker modules** (`src/platform/*`, `src/api/*`), **`migrations/`** (D1 schema), and **`docs/{backend-architecture,data-api-contract,cloudflare-deployment,phase-roadmap}.md`**. The static site / i18n / SEO are untouched. ⚠️ **D1/R2/KV bindings are COMMENTED in `wrangler.toml` on purpose** (a placeholder id would break the auto-deploy) — read the §CODEX HANDOFF entry below before touching them.

---

## 🤝 CODEX HANDOFF — current state (2026-06-07)

> **2026-06-19 (Codex) - Homepage Smart Cameras image brightness lifted.** User said the homepage Smart Cameras & Vision Platform card image felt too dark and asked to keep the existing filtered/industrial treatment while restoring more brightness, especially in the upper half. Scoped the card image wrapper in `astro/src/components/Home.astro` with `compact-image--vision` and added a targeted rule in `astro/public/home-sections.css` / `public/home-sections.css`: `brightness(1.14) saturate(1.04) contrast(1.03)` plus a very light top-half `screen` gradient overlay. This leaves other homepage product/resource images unchanged, preserves the card hover structure, and updates only the three homepage deploy files (`public/index.html`, `public/de/index.html`, `public/ja/index.html`) with `home-sections.css?v=dbe313b0`. Did not touch `public/shop*`. **Validation:** `PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADh5yZZyBs-zTw3Y npm run build` PASS; `npm run verify:all` PASS (i18n remains existing report-only); local `astro preview` Browser DOM/computed-style QA on `/#products` confirmed the target image uses `/home-smart-camera-platform-line.png`, computed `filter` is `brightness(1.14) saturate(1.04) contrast(1.03)`, the overlay is a `screen` top gradient, no framework overlay text, no horizontal overflow at the active desktop viewport, and console warn/error count is 0. Browser `Page.captureScreenshot` still timed out, and the Browser viewport override kept reporting 1280px for mobile, so this visual QA is DOM/computed-style based for the rendered page rather than screenshot-based.

> **2026-06-19 (Codex) - Corrected Products menu Machine Vision image to the true previous asset.** User pointed out the Products → Machine Vision Systems submenu image was still the wrong black horizontal scanner image. The earlier "restore" used the `d6ab57f` version, but the true pre-change menu image was the original `8f90cfa` blob: a front-facing machine-vision camera/lens over a metal-parts conveyor. Added it as a menu-owned, single-use asset `public/product-menu-machine-vision-systems.png` (hash/blob `633cda21ae7faf8559ae2ca8f46ddfc0b7ed6ad9`, matching `8f90cfa:public/product-smart-camera-detail.png`) and updated `astro/src/data/header-navigation.ts` so only the Products Machine Vision submenu uses it. The homepage Smart Cameras card still uses `public/home-smart-camera-platform-line.png`; no image slot shares this menu asset. Synced the header change across rendered `public/**/*.html` because the header is rendered on every page. Did not touch `public/shop*`. **Validation:** `PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADh5yZZyBs-zTw3Y npm run build` PASS; `npm run verify:all` PASS; Browser DOM QA on local `astro preview` confirmed the Machine Vision submenu computed `--submenu-image` is `/product-menu-machine-vision-systems.png`, `/product-smart-camera-detail.png` is no longer in the header HTML, desktop page has no horizontal overflow and no console warn/error; local HTTP checks returned 200 for `/product-menu-machine-vision-systems.png` and `/home-smart-camera-platform-line.png`.

> **2026-06-19 (Codex) - Smart camera image ownership split + single-use image rule.** The Products mega-menu Machine Vision panel unexpectedly changed because the previous task replaced the shared `public/product-smart-camera-detail.png` asset in place; that file was used by both the homepage Smart Cameras & Vision Platform card and the Products menu panel. Restored `public/product-smart-camera-detail.png` to the pre-replacement version from `d6ab57f35b54f2732de8eb6db655c0e9259bd97d` (hash `7a461eabdee143ac2a393378375725ee3abb5c8e`) so the menu gets its previous image back. Added a homepage-only asset, `public/home-smart-camera-platform-line.png`, for the user-supplied black scanner/conveyor image and updated only `Home.astro` + deployed homepage HTML to reference it. Also codified the new rule in `HANDOFF.md`, `docs/CONTENT_GUIDE.md`, and `docs/SAFETY_CHECKLIST.md`: content/marketing/product/navigation images are slot-owned and single-use; create a separately named file for each placement, with only baseline UI assets (logo/favicon/payment marks/social image/icons) allowed to be shared. Did not touch `public/shop*`. **Validation:** `PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADh5yZZyBs-zTw3Y npm run build` PASS; `npm run verify:all` PASS (i18n remains existing report-only); local `astro preview` Browser QA PASS on `/` at desktop and 390px mobile: homepage Smart Cameras card uses `/home-smart-camera-platform-line.png`, Products Machine Vision submenu keeps `/product-smart-camera-detail.png`, both assets return 200, desktop/mobile have no horizontal overflow, and console warn/error count is 0. The in-app browser's synthetic hover did not visually open the desktop mega-menu, so the submenu image check is DOM/computed-style based; homepage card rendering was screenshot-verified.

> **2026-06-19 (Codex) - Homepage machine vision card image replaced.** User supplied a new machine-vision line image (`C:\Users\Administrator\Downloads\ChatGPT Image 2026年6月19日 14_58_24.png`) and asked to replace the homepage Smart Cameras & Vision Platform image in-place, preserving the existing asset name/format. Replaced the tracked deployed asset `public/product-smart-camera-detail.png` with the new PNG; also copied the same bytes to local preview source `astro/public/product-smart-camera-detail.png`, but that path is intentionally ignored by `astro/.gitignore` as a duplicate static-media copy, so it is not committed. No Astro/CSS/layout code changed. Did not touch `public/shop*`. **Validation:** `PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADh5yZZyBs-zTw3Y npm run build` PASS; `npm run verify:all` PASS (i18n remains existing report-only); local `astro preview` PASS with Browser QA on `/` at 1440x900 and 390x844: target image currentSrc is `/product-smart-camera-detail.png`, natural size is 1447x1087, Smart Cameras card renders the new image visibly in desktop/mobile, no horizontal overflow, and no console warnings/errors. Observed an existing unrelated image placeholder on a neighboring card while scrolling; not caused by this replacement and not included in this scoped change.

> **2026-06-16 (Codex) - Homepage proof stats replaced with honest portfolio metrics.** User asked to revise the dark homepage proof/stat strip: remove customer count because GYUTRON currently has no customers to claim, judge whether the `ISO / Quality-managed production` metric is useful, remove the generic `30d Return and replacement window`, and preserve the original style/motion. After checking peer industrial automation sites (KEYENCE/Cognex/Zebra/SICK), the stronger and safer pattern is portfolio breadth + capability coverage, not unverifiable customers/certifications/return promises. Updated the four stats in `astro/src/components/Home.astro` to: `4 Integrated product lines`, `18 Catalog categories` (real product-category pages, excluding the three zero-product redirect stubs), `90+ Industrial product models` (93 local product models, rounded down for marketing display), and `3 Sales languages`. Updated en/de/ja labels in `astro/src/i18n/{en,de,ja}.json`. Also removed the remaining `ISO-managed production` sentence from the homepage About value copy and replaced it with documented checks / repeatable specifications / application-level validation, avoiding an unverified certification implication. Deployed only the three homepage HTML files (`public/index.html`, `public/de/index.html`, `public/ja/index.html`) from the current Astro build; CSS files were not changed, but the homepage HTML now carries build-current CSS cache keys so `dist` and `public` stay equivalent under the strict header gate. Did not touch `public/shop*`. **Validation:** `PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADh5yZZyBs-zTw3Y npm run build` PASS; `npm run verify:all` PASS (i18n remains existing report-only); static checks confirmed the old stat strings are gone from active homepage source/deploy files except unrelated `timeline.30days` form copy; Browser QA via local `astro preview` PASS at 1440x900 and 390x844 on `/`: proof stats render as `4 / 18 / 90+ / 3`, counter animation reaches the final `90+`, desktop grid remains 4 columns, mobile grid remains 2 columns, no horizontal overflow, no console warnings/errors. Screenshots captured successfully. Local preview still shows known About-section image gaps because some already-deployed image assets exist in `public/` but not `astro/public/`; not caused by this change and live public assets remain present.

> **2026-06-16 (Codex) - Product cards: remove card-level Contact Sales + square-rounded tags.** User asked to remove the `Contact Sales` button under all main-site product cards and make tags like `2D inspection` rounded-square instead of pill-shaped. Updated shared `ProductPage.astro` to remove `.product-actions` from every product card across en/de/ja product category pages; cleaned the now-unused `.product-actions` / `.product-action-*` CSS; changed `.product-tags span` from a 999px pill to a 6px rounded rectangle with a subtle purple border. Scope stayed on the main site only: `astro/src/components/ProductPage.astro`, `astro/public/product-page.css`, deployed `public/product-page.css`, and the 18 product category pages across en/de/ja (54 HTML files) with `product-page.css?v=afc84439`. Did not touch `public/shop*`. **Validation:** `PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADh5yZZyBs-zTw3Y npm run build` PASS; `npm run verify:all` PASS (i18n remains existing report-only); static grep confirmed no `.product-actions` / `.product-action-*` remain in the deployed product pages; Browser DOM/computed-style QA via local `astro preview` PASS at 1440x900 and 390x844 on `/area-scan-cameras.html`: product card `Contact Sales` links = 0, `.product-tags span` `borderRadius=6px`, product grid remains 3 columns desktop / 2 columns mobile, no horizontal overflow, no console warnings/errors. Browser screenshot capture timed out twice again, so visual QA evidence is DOM/computed-style based.

> **2026-06-16 (Codex) - Homepage Core capabilities right-copy aligned to title.** User asked the right-side intro copy to align with the left large title. Adjusted only `body.home .section-products .section-head p` desktop margin from 32px to 40px so the paragraph top aligns with the `h2` top while leaving the mobile stacked layout unchanged. Scope stayed narrow: `astro/public/home-sections.css`, deployed `public/home-sections.css`, and the three homepage HTML files only (`public/index.html`, `public/de/index.html`, `public/ja/index.html`) with `home-sections.css?v=1e554dbb`. Did not touch `public/shop*`. **Validation:** `PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADh5yZZyBs-zTw3Y npm run build` PASS; `npm run verify:all` PASS (i18n remains existing report-only); Browser DOM/computed-style QA via local `astro preview` PASS at 2048x900 and 390x844: desktop right copy top aligns with H2 top (`pTopMinusH2Top=-0.3px`), mobile remains stacked (`pTopMinusH2Bottom=28px`) with no horizontal overflow, and console error/warn count 0.

> **2026-06-16 (Codex) - Homepage Core capabilities right-copy frame removed.** User asked to remove the visible box/frame around the right-side "Three capability areas..." intro copy. Kept the improved top alignment and text styling, but removed the note treatment: no top rule, no left rule, no gradient background, no extra padding. Scope stayed narrow: `astro/public/home-sections.css`, deployed `public/home-sections.css`, and the three homepage HTML files only (`public/index.html`, `public/de/index.html`, `public/ja/index.html`) with `home-sections.css?v=809116dc`. Did not touch `public/shop*`. **Validation:** `PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADh5yZZyBs-zTw3Y npm run build` PASS; `npm run verify:all` PASS (i18n remains existing report-only); Browser DOM/computed-style QA via local `astro preview` PASS after cache-busted reload at 2048x900 and 390x844: intro copy `borderTopWidth=0px`, `borderLeftWidth=0px`, `backgroundImage=none`, `paddingTop/Left=0px`, no horizontal overflow, and console error/warn count 0. Browser screenshot capture timed out twice again, so visual QA evidence for this tiny tweak is DOM/computed-style based.

> **2026-06-16 (Codex) - Homepage Core capabilities CTA color + right-copy alignment.** Follow-up on the homepage `section-products` visual pass: matched the "Talk to an automation engineer" CTA to the header Contact Sales button by using `--brand-purple #4b2e83` and the same hover color/shadow behavior, then changed the right-side "Three capability areas..." copy from bottom-aligned paragraph text into a compact technical note with top/left rule treatment. This removes the large blank area above that copy while keeping the section hard-edged/industrial. Scope stayed narrow: `astro/public/home-sections.css`, deployed `public/home-sections.css`, and the three homepage HTML files only (`public/index.html`, `public/de/index.html`, `public/ja/index.html`) with `home-sections.css?v=3313b244`. Did not touch `public/shop*`. **Validation:** `PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADh5yZZyBs-zTw3Y npm run build` PASS; `npm run verify:all` PASS (i18n remains existing report-only); Browser QA via local `astro preview` PASS at 2048x900 and 390x844: CTA computed background matches header (`rgb(75, 46, 131)`), no horizontal overflow, desktop copy top is aligned near the title first line, and mobile layout remains single-column with two-column tags.

> **2026-06-16 (Codex) — 首页 Core capabilities 间距压缩 + 部署行业标签工业化.** 针对用户截图反馈，给首页能力区 `<section id="products">` 增加 `section-products` 作用域（`astro/src/components/Home.astro`），只压缩该段顶部留白与标题到标签的间距，不影响全站 `.section`。`astro/public/home-sections.css` 中把 `Deployed across` 标签组从浅紫色 chip 改为硬边工业信息栏：深色 label + 白灰金属铭牌式标签 + 左侧内嵌紫色识别线，桌面宽屏 CTA 与标签同排，移动端保持两列标签与全宽 CTA。**部署同步范围刻意很小**：只同步 `public/home-sections.css` + 三语首页 `public/index.html`, `public/de/index.html`, `public/ja/index.html`（更新 `home-sections.css?v=8cfcab91`），未触碰 `public/shop*`。**验证：** `PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADh5yZZyBs-zTw3Y npm run build` PASS；`npm run verify:all` PASS（header/sitemap/routes/seo/a11y-lite hard gates 绿；`verify:i18n` 仍为既有 report-only 残留）；本地 `astro preview` + Browser DOM QA：2048×900 宽屏 `feat-bar` 高 46px、无横向溢出、CTA 右侧同排；390×844 移动端 `scrollWidth=390`、无横向溢出、标签两列/奇数项全宽。⚠️ Browser 插件的 `Page.captureScreenshot` 连续超时，且本机无可用 Chrome/Edge、未下载 Playwright 浏览器依赖；本次视觉 QA 以 DOM/样式测量 + 构建验证为准。既有行尾/缓存戳烂账仍未根治，留给单独整站 LF 归一化任务。

> **2026-06-15 (Claude) — 移动端产品页上线 + 本地源码收敛 + 行尾烂账发现.** `ProductPage.astro` 的 CTA 改为 full/short 双 `<span>`（移动端短标新 key `pp.card.ctaShort` = Quote/Angebot/見積），`product-page.css` 移动端 hero 压缩 / 分类横向滑动 / 产品两列 / 短 CTA 切换。**外科式同步**：只把 **54 个产品分类页(18 类 × en/de/ja) + product-page.css** 同步进 `public/`（其余页未动），`npm run verify:all` 全绿（header/sitemap/routes/seo/a11y-lite PASS；i18n report-only 既有残留）。⚠️ **行尾烂账（待根治，单独任务）**：仓库 CSS 的 git blob 是 **CRLF**，但线上缓存戳 `?v=` 是**混合**哈希——`nav-chrome`/`chat-widget` 当年用 LF 算、`mobile-navigation`/`contact-page` 用 CRLF 算（历史构建工作区行尾不一致所致）。`mobile-navigation.css` 全局加载 → 任何干净重建都会让它 `?v=` 变 → 牵动整站 124 页 HTML（**纯缓存戳 churn、无功能影响**，`?v=` 仅浏览器缓存键、服务器忽略）。本次产品页用全 LF 哈希自洽部署，未碰其余页。**要彻底消除**：加 `.gitattributes`（`* text=auto eol=lf` + 图片 binary）→ `git add --renormalize .` 把 CSS 统一为 LF blob → 整站重构建重部署一次（124 页缓存戳变更）。⚠️ 依赖用 **npm**（本仓连 `package-lock.json` 都不提交）；pnpm 仅本地装依赖应急用，`pnpm-lock.yaml`/`pnpm-workspace.yaml` 已加入 `.gitignore`、**绝不提交**。本地权威路径见 §1（已修正为 `D:\AI PRODUT\GYUTRON web\website\gyutron-website-repo`）。

> **2026-06-10 (Claude) — TURNSTILE LIVE + Phase-4 shop behavior beacon.** **Turnstile evidence (P0):** site key `0x4AAAAAADh5yZZyBs-zTw3Y` baked at build (`ad01433`, widget verified in live HTML en/de/ja on all 4 forms), boss set `TURNSTILE_SECRET_KEY` → tokenless POSTs to /api/rfq + /api/contact now **400 "Anti-spam verification failed"** (live-tested); human-pass test = boss browser submission (pending confirmation). Rollback: delete the secret → graceful skip. **Phase-4 (P2) shop BEHAVIOR events** (NOT an order connector): Worker injects `/shop-analytics.js` into shop.gyutron.com HTML via HTMLRewriter (zero edits to generated shop files); the script is strictly minimal — product.viewed (product.html?sku=) + cart.added (Storage.setItem wrap diffing the localStorage cart) → `POST /api/shop-event` (both hosts; whitelist + rate-limit + NO ip/session/PII stored) → events table with **source=gyutron-shop** → Data API → agent normalizes into `cart_events`. emitEvent now accepts a source override. Boss ran `scripts/activate-backend.ps1`: **D1 `gyutron_db` (d79a8073…) + RATE_LIMIT KV bound and COMMITTED (`91b1bb0`)**; secrets DATA_API_KEY / ADMIN_PASSWORD / IP_HASH_SALT set. ⚠️ **R2 still commented — the account has NOT enabled R2** (API code 10042): enable R2 in the Cloudflare dashboard → `npx wrangler r2 bucket create gyutron-assets` → uncomment `[[r2_buckets]]` → commit+push. **Full chain verified live 2026-06-10:** 4 test submissions (LEAD-20260610-EKK369 / RFQ-…-MDPJNT / SUP-…-3WZVXJ / DL-…-WAZWP3, status=new, left visible for /admin review) → D1 rows + 4 events → `/api/v1/*` (Bearer) → Agent-Workspace `gyutron_website` connector synced 8/8 → bilingual Website Leads Summary generated from real rows. Agent backend reads `GYUTRON_WEBSITE_API_KEY` from the user env (`setx`, so only NEW processes see it). Still TODO: Turnstile widget (build-time site key, docs §5), R2 enablement + real datasheet uploads (then flip manifest access_levels), boss marks the 4 test rows closed/spam in `/admin`.
> **2026-06-10 (Claude) — Phase-2: form pages + Turnstile + resource-center MVP (P1–P3, P5).** Built on the Phase-1 backend (entry below). **(P2) Three NEW form pages ×3 locales** (en/de/ja, 132 pages total now): `/request-quote.html` (RFQ → POST /api/rfq; product-category/industry/timeline selects, URL-param prefill `?productCategory=<slug>`), `/support/contact.html` (→ POST /api/support; issue-type enum select), `/resources/download-request.html` (→ POST /api/download-request; document select fed by the resource-center manifest + "Other" free-text toggle). Components mirror `ContactSales.astro` (contact-page.css, info-panel+form-panel, inline STATUS maps, honeypot `website`, sourcePage/locale/utm collected client-side in `lib/forms/meta.ts`). Business layer: `lib/forms/{rfq,support-request,download-request}.ts` + `ENDPOINTS` additions; types in `types/inquiry.ts`. **Entry points:** product-page cta-band now has a second `.button-outline` "Request a Quote" (purple-on-white override in product-page.css; prefills the category), SolutionPage CTA got the same secondary button, support pages (en/de/ja) got a 4th "Support request form" channel card, top-nav repoints (Get Help→`/support/contact.html` ×2; Resources: Specification Request→`/request-quote.html`, Application Notes + Compliance Documents→`/resources/download-request.html`), search index +3 entries/locale. **63 new i18n keys** (`rfq.* supc.* dlr.* fx.* seo.*`) with REAL de/ja. Pages registered in `data/pages.ts` (sitemap 41 paths ×3). **(P3) Turnstile is wired but DORMANT:** all 4 forms render the widget ONLY when **build-time** `PUBLIC_TURNSTILE_SITE_KEY` (astro/.env → `src/config/turnstile.ts`) is set — empty key = no widget, no script, unblocked submits (verified in DOM). Worker verifies on all 4 handlers (contact-handler now calls verifyTurnstile too) but SKIPS while the `TURNSTILE_SECRET_KEY` secret is unset. ⚠️ ROLLOUT ORDER: site key → rebuild → push → THEN secret (docs/cloudflare-deployment.md §5). **(P5) Resource center MVP:** manifest = `astro/src/data/downloads.json` (single source for the form select AND the worker; 7 seed entries, ALL `manual_review` on purpose — flip to gated/public only after real files are uploaded to R2 `gyutron-assets`). Worker `GET /api/download/:id` (src/api/downloads.mjs): public→stream from R2, gated→15-min HMAC token issued by the form response (`download.url`), manual_review→403; degrades without R2/secret. forms.mjs sets `access_type` from the manifest (server-side policy). **(P1) `scripts/activate-backend.ps1` rewritten:** fixed PS5.1 `2>&1`+EAP=Stop kill (proxy warnings on stderr), useless whoami exit-code check, d1-create output regex; now ALSO creates R2 + KV and patches all three bindings. ⚠️ After the boss runs it, the patched `wrangler.toml` (real ids, NOT secrets) MUST be committed+pushed or the next push deactivates the backend. **Verified:** build 132p, verify:all GREEN (header gate re-baselined via dist→public sync, 135 files, shop untouched), i18n-gaps clean for new namespaces (supc "Support" de = intentional loanword), i18n-audit OK, smoke 20/20, wrangler dry-run OK, in-browser desktop+mobile (incl. prefill, submit pipeline, "other" toggle, outline-button contrast, no-widget assertion). **NEXT:** P4 Agent-Workspace connector (agent repo) + boss activation + full-chain acceptance. A backend was added to the existing Worker WITHOUT touching the static site, i18n, SEO, or the shop. **What landed:** (1) **D1 schema** `migrations/0001_init.sql` — `leads / rfqs / support_requests / download_requests / events / api_keys` (no `tenant_id`; one DB+Worker per customer). (2) **Reusable, customer-agnostic core** `src/platform/*` (config identity, response envelopes, public_id, validation, `schemas.mjs` = single source of truth for form fields + status vocab + column allow-lists + Data-API projections, CSV, injection-safe D1 repository, events, auth, admin session, Turnstile, rate-limit). (3) **Form APIs** `POST /api/{rfq,support,download-request}` in `src/api/forms.mjs`; **`src/contact-handler.mjs` now ALSO persists to D1 + emits an event** (Resend email kept; success if persisted OR emailed). (4) **Read-only Data API** `GET /api/v1/*` (`health` public; `metadata` + 5 resources + `products`/`categories` behind Bearer/X-API-Key) in `src/api/{data,products}.mjs` — for the **Agent Workspace** to consume (it reads the API, never the DB). (5) **Internal admin** `/admin/*` (`src/api/admin.mjs`) — single-admin password, list/detail/status/note/spam/delete/CSV, server-rendered, noindex. (6) **Routing**: `src/worker.mjs` dispatches `/api/*` + `/admin/*` on the brand host only; shop block + `ASSETS` fallthrough are byte-identical. **GRACEFUL DEGRADATION (critical):** with no `DB`/`R2`/`RATE_LIMIT`/`TURNSTILE_SECRET_KEY`/`DATA_API_KEY`/`ADMIN_PASSWORD` the new endpoints self-disable (503/401/skip) and the static site + contact email keep working — which is why the D1/R2/KV bindings are **commented** in `wrangler.toml` (an active placeholder id fails deploy). **To activate:** `wrangler login` → create D1/R2/KV → uncomment the matching binding + paste the real id → `wrangler secret put …` → `wrangler d1 migrations apply` → deploy. Full steps in **`docs/cloudflare-deployment.md`**; architecture in **`docs/backend-architecture.md`**; API spec in **`docs/data-api-contract.md`**; scope in **`docs/phase-roadmap.md`**. **Verified:** `node scripts/smoke-platform.mjs` (17 pass), `wrangler deploy --dry-run` (bundles, 65 KiB gzip), `astro build` (unchanged). **5-lens adversarial review** found no critical/high; fixed the mediums (Data-API scope enforcement, admin-login rate-limit, products redirect-alias dedup) + lows (contact 502-vs-503 + null-body guard, public /health no longer leaks capabilities). Reviewed against AGENTS.md UTF-8 / commit / deploy protocol. **Do not** uncomment a binding with a placeholder id, and **do not** add Turnstile's secret before the form has the widget (would break the live contact form).

> **2026-06-09 (Claude) — i18n translation architecture + nav/new-content de+ja.** **Rule:** visible content (nav / footer / pages) must have REAL de+ja — NOT English placeholders. New keys may ship en-real / de-ja-placeholder, but a content area is not "done" until translated. **Tooling = `astro/tools/i18n-gaps.py`**: finds keys where `de==en` or `ja==en` (excluding intentionally-English tokens — GYUTRON / GY-* / RFID / SPI / AOI / AXI / SDK / API / EMS / PCB / SMT / units… via its `KEEP_EN` allowlist). `python tools/i18n-gaps.py` = summary by namespace; `--list <prefix…>` = list gaps; `--dump <prefix…>` = emit `{key: en}` to translate. **Process:** dump gaps → translate (keep glossary terms English) → write back ONLY where current value still == en (never clobber real translations) → re-run gaps until clean. This session translated all **`ind.*` (Industries) + `sol.*` (Solutions)** to de+ja via a translation workflow. Residual gaps (`main.*` ~12, `home`, a couple of others) are mostly intentional-English or tiny and can be swept the same way.

> **2026-06-09 (Claude) — top nav cut 6 → 5 items (de-duplicated Customers/Company/Resources).** After a peer scan (Keyence/Cognex/Basler/SICK/OMRON/Zebra/LUCID — none run a "Customers" tab, all merge Support+Resources and demote Company), the header is now **Products · Solutions · Industries · Support · Resources**. **"Customers" and "Company" were removed as top items.** **Support** (new, `fnav.support` label) = owner-facing service + downloads/docs, built by recombining the existing keys: Services & Lifecycle Support (main.152) · Technical Documentation (main.229) · Training & Enablement (main.162) · Get Help (main.247, relabeled from "Support Resources"). **Resources** (main.220, rebuilt) = Knowledge Center (main.221) · Buying Tools (main.237) · Plan Your Project (main.142, relabeled from "Customer Success"). **Company is footer-only now** — its column in `FOOTER_NAV` (navigation.ts) is its home; added Partner Programs (main.172) there so partners aren't lost. Almost pure key-recombination — only main.142 & main.247 were relabeled (en/de/ja). Orphaned-but-harmless keys remain (ex-Company main.183-219, ex-Customers Partner main.174-181). `verify:all` GREEN (navItem=5). **Follow-up same day:** added a **Case Studies** group to Resources (`nav.cases.*` keys, fully translated en/de/ja) → Resources now Knowledge Center · Case Studies · Buying Tools · Plan Your Project; and curated the footer **Company** column to About · Careers · Newsroom · Partner Programs · Contact (dropped Certifications/Downloads — reachable from the Support/Resources nav — to keep the column from outgrowing the others). **Note for later (asked, not done):** the footer **Support** column still mixes real support links with Privacy/Terms — those legal links should move to the bottom copyright/legal row; footer ≠ nav is fine by design, just de-mix legal from Support.

> **2026-06-08 (Claude) — Industries menu rebuilt as a data-driven sector registry.** The top-nav **Industries** mega-menu was redone (with the user) around 4 broad sectors spanning GYUTRON's full range (vision + robotics + AGV/AMR + automation): **Electronics & Semiconductor Manufacturing · EV Battery & New Energy · Logistics & Warehouse Automation · General Manufacturing & Industrial Automation**. New registry **`data/industries.ts`** (`types/industry.ts`): each industry has a `coverage[]` list of the areas it covers (e.g. 3C, PCB/PCBA, SMT, EMS, AGV/AMR, machining…). `data/header-navigation.ts` derives the Industries groups from it via `.map()` (mirrors `solutionGroups`) — **don't re-hardcode it.** Copy lives in the **`ind.*`** i18n namespace (en real; **de/ja = English placeholder**, like `sol.*`; generator `_wire_industries.py`). The flyout reuses `.submenu--solution` but, being the only **compact** one, renders the coverage as **title-only wrapped chips** via a scoped `nav-chrome.css` rule (`MegaSubmenuLink.descKey` is now optional → `MegaMenuLink.astro` renders the desc `<span>` only when present), plus a black **`detailKey`** description paragraph (`.submenu-detail`) under the chips that fills the lower half of the panel. Industry pages don't exist yet → `industry.path` → `/index.html#solutions`; build per-industry pages later, then update `path` (+ add to search/sitemap). **2026-06-09 follow-ups:** (a) the flyout got a black `detailKey` description paragraph under the chips (fills the lower half) — the Industries flyout is absolutely positioned inside a fixed-height panel, so it's capped with `max-height:100% + overflow-y:auto` (and a slightly shorter `::before` banner) so oversized text — a visitor with a large/min font size — scrolls inside the flyout instead of spilling out the panel bottom; (b) **all local CSS links are now content-hash cache-busted** — `Layout.astro` wraps every `pageCss` href + `/nav-chrome.css` in **`lib/cssVersion.ts` `withVersion()`** (build-time SHA1 of the file → `?v=<hash>`), because the CSS had NO cache-buster and a browser holding a stale `nav-chrome.css` rendered the new flyout broken (detail text clipped). The hash updates automatically on every CSS edit — nothing to bump by hand; just rebuild. Also **fixed a stale gate**: `scripts/verify-header-equivalence.mjs` didn't count `submenu--solution` (so `verify:header` had been RED since the Solutions flyout shipped) — regex now includes it; `verify:all` GREEN. `verify:i18n` flags the new `ind.*` English in de/ja (report-only, expected). (Earlier this day the menu had a narrower 4-industry production-line/`stations[]` shape — superseded by this sector model.)

A large UI + architecture overhaul shipped this session. **Everything below is LIVE**; `local == origin` at HEAD **`1e2a24f`** (run `git fetch` and confirm `HEAD == origin/main`, then continue). Codex now owns the task. **Read `docs/MAINTENANCE.md` (task→file index) + `docs/SAFETY_CHECKLIST.md` before editing.**

**What shipped this session (don't redo / don't revert):**
1. **Verification gates + CI.** `npm run verify:all` = build-gated **verify:header / verify:sitemap / verify:routes / verify:seo / verify:a11y-lite** (+ `verify:i18n` & `verify:assets` = report-only). GitHub Actions (`.github/workflows/verify.yml`) runs them on push/PR to main — **it never deploys**. Scripts live in `astro/scripts/*.mjs` (plain Node, no deps).
2. **Engineering layers (mostly additive).** `lib/cms` local facade (delegates to `src/data`), `lib/crm` + `lib/agent` mock interfaces, asset registry `data/assets.ts`, **page registry `data/pages.ts`** (sitemap derives static pages from it). `lib/*` integrations stay **mock** — see FUTURE_INTEGRATIONS.md.
3. **Solutions data model** (`types/solution.ts` + `data/solutions.ts` → `SolutionPage.astro` renders from data).
4. **Homepage rebuilt below the hero** (`Home.astro` + `home-sections.css`, all `body.home`-scoped — `global.css` untouched): industries trust strip → dark stats band (counters) → capabilities (6 **distinct product-family** cards) → OEM/ODM band → industries TabCarousel → platform/why-us → "how we work" process → **About/company** (intro+values+facility imgs) → **Partners/ecosystem** → **Newsroom** → CTA. Motion = scroll-reveal (Apple-style rise+scale, `--rd` stagger) + stat counters + auto-scroll marquee; **only card items carry `[data-reveal]`** (no nested → no flicker); the **CTA/contact band must NOT animate**.
   - **2026-06-07/08 Codex motion pass:** Dark purple full-width bands (`proof-band`, `hx-oem`, `hx-partners`) no longer use whole-section opacity reveal, which avoids purple-background flicker. Partners/ecosystem changed from a static grid to an auto-sliding marquee wall that still uses honest standards text tiles from `data/partners.ts`; do **not** add fabricated customer/partner logos. Real logos can be added later via `PARTNERS[].logo`. A later `hx-scroll-story` / "System layer" experiment was removed on 2026-06-08 because it duplicated nearby platform/process content and created spacing risk; **do not restore it unless the user explicitly asks.**
5. **News system.** `/news` index + `/news/<slug>` article pages (en/de/ja) from `data/news.ts` (`NewsIndex.astro` / `NewsArticle.astro`); homepage Newsroom + search link to articles; registered in `data/pages.ts` → sitemap (now 93 URLs = 31 paths × 3). Seed posts are factual — replace with real ones.
6. **Search cards.** Product / solution / news results render as image cards (`nav-search.js` `hitHtml` + `searchIndex.ts` records gain `i`/`c`); support/pages stay text rows. Header markup unchanged (cards rendered via JS).
7. **Content & i18n.** Fixed residual English in `products.{de,ja}.js`; de-duplicated overlapping homepage copy (Products / Solutions / Platform / About) + the 6 solution cards (each now a distinct product family). **Brand color rule below.**

**NON-NEGOTIABLE RULES (new this session + reaffirmed):**
- 🟣 **PURPLE-ONLY.** Accent = light purple `--purple-500 #8a63d2` (paler tint `--violet-soft #efe8ff`); **NEVER green/teal/cyan** (the old `#00c2a8`/`#00d2be`/`#18b6c9` were removed). Functional red (errors) + amber (`--warning`) may stay. Retune via the `--signal`/`--hero-accent`/`--hx-accent`/`--purple-*` tokens. See §2.
- 🧱 **Hard-edged industrial.** `border-radius: 0`; heavy + tight headings; brand text uppercase. Reference: thinklucid/onlogic. All motion `transform`/`opacity` only + respects `prefers-reduced-motion`.
- 🚫 **HONESTY — NEVER fabricate** stats, claims, client counts, founding dates, specs, or partner logos. Only true, verifiable GYUTRON facts. (A prior AI draft invented "since 2010 / 200+ clients / 18-month lead time / managed fleet" — all rejected. Don't reintroduce them.)
- 🖼️ **IMAGE SINGLE-USE RULE (reaffirmed 2026-06-19).** Every content/marketing/product/navigation image slot must have its own purpose-named asset file. Do **not** reuse one content image in two places (homepage card + menu panel, product-page hero + nav panel, SKU + category hero, etc.). If two slots need a similar visual, create a separate file for the second slot and wire only that slot to it. Baseline UI assets such as the logo, favicon, payment marks, social/share image, and icon libraries are the only shared-asset exceptions.
- 🌐 **i18n.** Every visible string needs en+de+ja or the build throws (`t()` gate). Never translate model names (`GY-*`). Category fields in `products.{de,ja}.js` are hand-translated (verify:i18n only scans rendered pages). Same-page language switch.
- 📦 **Deploy = committed `public/`.** After a change: `npm run build`, then sync ONLY the changed `astro/dist/*` into `public/` (check `diff -rq astro/dist public`); **never bulk-copy dist→public, never touch `public/shop*`**. `dist/` and `package-lock.json` are gitignored (CI uses `npm install`).
- 🧩 **Header DOM is a byte-contract** → run `npm run verify:header` after ANY header/nav change. Don't revert data-driven modules to hardcoding.
- ✅ **Per change:** `npm run build` + `npm run verify:all`; commit per change with **specific paths** (not blind `git add -A`); before commit/push re-check no other agent is mid-push and `HEAD == origin/main`.

**STALE rules deeper in `AGENTS.md` — IGNORE where they conflict:** the "colored-bars hero" rule (hero is 3 layouts, no bars); the `locales/` + `npm run i18n:build` / `i18n:sync` / `tools/generate_localized_site.py` generator (NEVER run — it CLOBBERS the Astro site); "de is a homepage-only sample" (all pages are Astro-localized en/de/ja); the homepage Solutions-card taxonomy (the 6 homepage cards were re-scoped to distinct product families 2026-06-07 — the header Solutions mega-menu in `data/header-navigation.ts` is the nav taxonomy source).

**Remaining TODO (pick up here):**
- Replace the 3 **seed Newsroom posts** in `data/news.ts` with real posts (set `body`, `image`, keep `href: /news/<slug>.html`).
- Add real **partner/customer logos** in `data/partners.ts` (`logo` field) only after the relationship is true/approved — currently honest interface-standard text tiles in the homepage sliding wall (no fabricated 3rd-party logos).
- Add a footer/header **"News" nav link** (deferred — Footer renders on every page, so it re-syncs all pages; news is reachable via homepage CTA + search + sitemap).
- Optional **industrial webfont** (a condensed grotesk) — needs a self-hosted font file or a CDN decision (conflicts with the self-contained/perf convention); CSS heading-hardening already applied.
- `.nav-trigger` **`aria-expanded`** (a11y) — high blast radius (all-page header + the per-page nav scripts + `mobile-navigation.js`); see ACCESSIBILITY.md.
- **CSS dedup** across the per-page CSS files; extend the global heading-hardening to non-homepage pages (the global heading rule only loads on the homepage today).

---

## 0. TL;DR — the 5 things that BREAK the site if you get them wrong
1. **The site is now ASTRO** (everything lives in the **`astro/`** subdir). The old root-level `*.html` pages + the Python generator are DEAD/legacy.
2. **NEVER run the legacy generator** — `npm run i18n:build`, `npm run i18n:sync`, or `tools/generate_localized_site.py`. It regenerates legacy pages into `public/` and **CLOBBERS the Astro site**. *(This contradicts old AGENTS.md lines 17–18 — ignore those.)*
3. **Deploy = build Astro → copy changed output into `public/` → commit → push.** Cloudflare serves the committed `public/`. See §3.
4. **shop.gyutron.com is OUT OF SCOPE.** Never touch `public/shop`, `public/de/shop`, `public/ja/shop`, or shop i18n keys.
5. **`t(locale,key)` THROWS on a missing key** (intentional build gate). Any new visible text needs **en + de + ja** or the build fails.
6. **Write de/ja TEXT as UTF-8.** Never write Japanese/German via a Python process without `PYTHONUTF8=1` / `PYTHONIOENCODING=utf-8` — it silently corrupts the characters to `?` (mojibake). *(This exact bug hit the new 900-series products' Japanese `type`/`summary` in `products.ja.js`.)* Edit the json/data files directly as UTF-8; after any de/ja data edit, `grep -nP '\?{4,}'` the file — it must be empty.
7. **Product data must be FULLY localized; run the audit.** Every product's `type`/`summary` and every category's eyebrow/title/navLabel/panelText/sectionIntro need de+ja translations (this was a systemic gap — ~60 products + many category fields shipped English). **Run `python tools/i18n-audit.py`** after any `products.*.js` change (exit 0 = clean; catches untranslated / partial-English / mojibake / renamed-model-name). **NEVER translate a product model NAME** — keep `GY-CR720 Conveyor` etc. identical in all locales (and matching the `getProductsByName` refs). `panelMetric` is legacy product-count data and is no longer rendered; do not add visible SKU/model counts to product hero panels because they become stale whenever products are added.
8. **Dual-agent sync + long-term architecture.** Claude AND Codex both develop this repo — `git fetch` then **READ this `HANDOFF.md` before every task**, and **UPDATE it after every task** (see §4). On every change follow the **long-term architecture principles (§11)** — componentize, data-drive, static/SEO/perf/i18n-ready, industrial visual, don't over-engineer.

---

## 1. What / where
- gyutron.com main site, rewritten in **Astro 5** (`output:'static'`, `build.format:'preserve'` so URLs byte-match the legacy site). 21 pages × 3 locales (en `/`, de `/de/`, ja `/ja/`) = **63 pages**. LIVE.
- GitHub **hc17604/gyutron-website** (public). Local working copy (唯一权威路径，2026-06-15 收敛后): `D:\AI PRODUT\GYUTRON web\website\gyutron-website-repo`. Astro project = `astro/`. *(旧的 `D:\Codex\workspaces\2026-05-17\…\gyutron-website-repo` 等重复克隆已删。)*

## 2. Design rule (hard requirement, all gyutron.com work)
**Industrial / rugged / hard-edged — NOT a soft "software-company" look.** (The lucid.co floating-cards style was rejected.) Reference: **thinklucid.com** — dark, product-forward, hard edges (border-radius 0), uppercase technical type, real imagery, square page dots. Use GYUTRON's own copy/images + **brand purple**: `--purple-500 #8a63d2` for accents on dark (the true `--brand-purple #4b2e83` is too dark to read on near-black). Keep GYUTRON brand text uppercase in visible copy.
- **⭐ COLOR RULE (hard, user 2026-06-07): PURPLE-ONLY.** The brand uses **only purple**. The secondary / accent color is **LIGHT PURPLE `--purple-500 #8a63d2`** (with paler tints `--violet-soft #efe8ff` for fills). **NEVER use green / teal / cyan** as an accent (the old `#00c2a8` `--signal`, `#00d2be` hero-accent and a `#18b6c9` homepage accent were all removed and mapped to purple on 2026-06-07). Functional **red** (errors) and **amber** (`--warning`) may stay; only the green/teal "second color" is banned. New work must not introduce non-purple accents — reuse `--signal` / `--hero-accent` / `--hx-accent` / `--purple-*` tokens.

## 3. Deploy procedure (EVERY change)
1. Edit `astro/src/**` (components/pages/data/i18n) or `astro/public/**` (static CSS/assets).
2. **`cd astro && npx astro build`** → builds 63 pages. A missing i18n key throws here = good gate.
3. **Sync changed build output into `public/`** (only `public/` deploys; never touch shop subtrees):
   - `cp astro/dist/index.html public/index.html`
   - `cp astro/dist/de/index.html public/de/index.html`
   - `cp astro/dist/ja/index.html public/ja/index.html`
   - plus any changed `astro/dist/<file>.css` / `_astro/<hashed>` → same relative path in `public/`.
4. Guard (§4), then commit `astro/` + `public/` and push to `main` → **Cloudflare auto-deploys (~1–2 min, no build step on Cloudflare)**.
- Cloudflare Worker: `wrangler.toml` → main=`src/worker.mjs`, `[assets] directory="./public"`, `run_worker_first=true`. Worker maps `/api/contact`→`contact-handler.mjs`, `shop.gyutron.com`→`/shop/` `/de/shop/` `/ja/shop/`, everything else → `env.ASSETS.fetch`. Cloudflare Assets default `html_handling` 307-redirects `/x.html`→`/x` (clean URLs) — pre-existing, harmless.
- **Media** (png/jpg/webp) are git-tracked in BOTH `public/` (deployed) and `astro/public/` (build/preview source) — keep both in sync. 72 product renders at `public/product-images/`; transparent product cutouts at `public/product-cutouts/`.

## 4. Concurrency + commit protocol
- More than one agent (and a human) may push. **Before any edit AND immediately before every push:** `git fetch` and confirm `git rev-parse HEAD == origin/main`. Reconcile if diverged.
- Commit PROMPTLY — don't leave uncommitted work a blind `git add -A` could sweep up. Prefer **`git add -- astro/ public/`** (plus explicit doc paths), not blind `-A`.
- Never kill `node` (agent tooling runs on node). Windows quirks: git's "LF will be replaced by CRLF" warning is harmless; PowerShell shows `git push` stderr as a red error even on success — trust the `local==origin` re-check, not the red text.
- **USER PREFERENCE: deploy directly, do NOT ask "deploy now?".** Still build + visually verify (desktop + mobile) + run the guard, then commit + push autonomously.
- **🔄 DUAL-AGENT MD SYNC (Claude + Codex both develop this repo).** This `HANDOFF.md` is how the two agents hand off — keep it the LIVE source of truth, not a stale snapshot. **BEFORE every task:** `git fetch` + RE-READ this file (+ the `AGENTS.md` banner) — structure/keys/state drift between sessions (e.g. nav scrollbar CSS got centralized into `nav-chrome.css`; `Layout.astro` moved to `src/layouts/`). **AFTER every task:** UPDATE this file in the SAME commit — bump "Last updated", and reflect what changed (new/moved components, new data files, new i18n keys, new gotchas, completed/added TODOs) so the other agent never works from a stale map. Keep doc edits surgical.

## 5. Architecture
- **i18n**: dicts `astro/src/i18n/{en,de,ja}.json`; `src/i18n/index.ts` exposes `t(locale,key)` (THROWS on missing), `localizeUrl(locale,path)`, `ogLocale/htmlLang/...`. New text → **reuse existing translated `main.*` / `seo.*` / `home.*` keys** where possible; if you must add a key, add it to **all three** json files.
- **Layout**: `astro/src/layouts/Layout.astro` (per-locale canonical + hreflang + og, ja font block, loads page CSS via `pageCss` + shared chrome). **Components** (`astro/src/components/`): `Header`, `Footer`, `LangSwitch`, `Home`, `ProductPage`, `SolutionPage`, `ContactSales`, `MetaRedirect`, **`HeroSlider`**, `TabCarousel`, `PaymentMethods`, `CompanyAddresses`, `SupportPage`. *(Toward the long-term structure — §11 — components may later group into common/home/product/solution subfolders; migrate incrementally + announce.)*
- **Products**: build-time from `src/data/products.{en,de,ja}.js` via `pages/[category].astro` (+ `de/` + `ja/`). Per-locale files (title/intro/summary/type translated). **`specs`/`tags` are canonical English** — the spec object KEYS double as compare-table identifiers (`s.Scan`/`s.RFID`…) so they MUST stay English — and are **localized at RENDER time by `src/lib/productI18n.ts`** (`specLabel/specVal/tagText`: maps the 85 field labels + descriptive values + descriptive tags → de/ja, with **English passthrough** for anything unlisted, incl. universal tech notation like Android 14 / IP68 / GigE / Wi-Fi 6E / mAh / MP / protocols which intentionally stays English). Used by `ProductPage.astro` + `HeroSlider.astro`. **Adding a product with a NEW descriptive value/tag → add one row to the matching map in `productI18n.ts`** (it's UTF-8 — never write its ja/de via non-UTF-8 Python). 3 redirect categories: smart-cameras→area-scan-cameras, industrial-sensors→proximity-sensors, inspection-instruments→dimensional-gauges.
- **2026-06-05 product hero maintenance:** `ProductPage.astro` no longer renders `cat.panelMetric` in the hero overlay. Keep product-page hero panels descriptive (`panelText`) rather than numeric, so adding/removing SKUs does not require manual count updates across en/de/ja. `tools/i18n-audit.py` intentionally ignores `panelMetric`; treat existing values as legacy data unless a later cleanup removes them completely.
- **2026-06-06 product listing compaction:** `ProductPage.astro` no longer renders the visible product-list `section-head` (`PRODUCT LINE`, `sectionTitle`, `sectionIntro`) above product cards because the hero already carries the page description. It keeps only a visually hidden `<h2>` for structure/SEO, and `product-page.css` compresses the product-list section top padding. Do not reintroduce visible `sectionIntro` blocks on product pages unless the user explicitly asks; this affects en/de/ja automatically through the shared Astro component.
- **CSS** in `astro/public/` (loaded via Layout `pageCss` plus shared chrome): `global.css`, `product-page.css`, `solution-page.css`, `contact-page.css`, `mobile-navigation.css`, `home-sections.css`, `hero-slider.css`, `nav-chrome.css`, `support-page.css`.
- **Site search (static, no backend) — added 2026-06-06.** Build-time per-locale index endpoints `src/pages/search-index.json.ts` (+ `de/`, `ja/`) call **`src/lib/searchIndex.ts`** → JSON of products + categories + key pages (localized via `localizeUrl`; products link to their category page; redirect categories skipped). Client: **`public/nav-search.js`** (loaded by `Layout.astro` BEFORE `mobile-navigation.js`) exposes `window.GyutronSearch`; renders a header magnifier → full-width dropdown on desktop and a search box atop the mobile drawer (`mobile-navigation.js`). Chrome strings = inline `SEARCH_I18N` map in `Header.astro`; styles in `nav-chrome.css` (`.nav-search*`); desktop `.nav-search` hidden ≤1180px. **To index more pages, add to `searchIndex.ts` PAGES; new products/categories are covered automatically.**
- **Shared chrome rule (hard):** Header/Footer/nav/language/global interaction changes must be treated as cross-locale AND cross-page-type work. The same `Header.astro` renders en/de/ja, but different page types load different CSS (`global.css`, `product-page.css`, `contact-page.css`, etc.). Do not assume an English homepage CSS fix reaches Japanese/German product pages. Either move the behavior into a truly shared stylesheet/component, or update every stylesheet that controls that shared UI, then verify at least one en page, one de page, one ja product page, and contact-sales before shipping.
- **Footer (ONE canonical component — consolidated 2026-06-03):** **`Footer.astro` is the single rich footer used by EVERY page** — `Home`, `ContactSales`, `ProductPage`, `SolutionPage` all do `<Footer slot="footer" locale={locale} />` (Home & Contact NO LONGER inline their own footer; this fixed a drift where product/solution footers lagged the homepage). Edit the footer in ONE place now: `Footer.astro`.** — **4 columns: Brand · Products+Solutions · Support · Company** (Solutions is a 2nd `<h4>` sub-head with inline `margin-top:28px` in the SAME column as Products; Company keeps About/Certifications/Downloads/Contact + email + WhatsApp + Official Store + **`<PaymentMethods />`**; the `socials` icon row stays in `footer-bottom` — **styled in `nav-chrome.css`** (plain glyphs, NO boxes, 24px gap, 19px icons), shared across ALL pages so it no longer drifts; the old boxed `.socials` rules were removed from `global.css` + `contact-page.css`). The footer LinkedIn *text* link was removed (redundant with the `socials` LinkedIn icon). **`PaymentMethods.astro`** renders one independent SVG per brand from **`/images/payment/{visa,mastercard,amex,unionpay,paypal}.svg`** (aaronfagan `flat-rounded` colored cards; swap/add a file to change a brand; keep `astro/public/images/payment/` + `public/images/payment/` in sync). Under the brand blurb the rich footer also renders **`<CompanyAddresses />`** (HK HQ + Shenzhen R&D), text via i18n keys **`footer.hqLabel/hqAddr/rdLabel/rdAddr`** (en/de/ja, fully localized incl. addresses) — add an office = add its 2 keys to all 3 dicts + a row in the component's `offices` array. **Footer nav labels use the `fnav.*` i18n namespace** (en/de/ja; the shop already owns `footer.shipping`/`footer.terms`/`footer.col.support`, so don't reuse those). ✅ **Support pages BUILT (EN, 2026-06-03)** via new **`SupportPage.astro`** template — `src/pages/support.astro` + `support/faq.astro` + `support/warranty.astro` + `support/shipping-delivery.astro` + `privacy-policy.astro` + `terms-and-conditions.astro` → `/support.html` etc. (industrial dark hero with optional **`heroImage`** prop — per-page background reuses existing repo assets: faq=`/product-vision-cell.png`, support=`/nav-company-engineering-lab.png`, warranty=`/nav-company-quality-bench.png`, shipping=`/nav-industry-logistics-rfid.png`, privacy+terms=clean dark/no image; a heavy dark gradient overlay keeps text readable. FAQ is grouped into 5 category `<h2>`s of native `<details>` accordions; styles in `support-page.css`; content is hardcoded per-locale in the page files, NOT in the i18n dicts). **Built in all 3 locales (en/de/ja) as of 2026-06-03** — de/ja are SEPARATE page files (`src/pages/{de,ja}/support.astro`, `…/support/{faq,warranty,shipping-delivery}.astro`, `…/privacy-policy.astro`, `…/terms-and-conditions.astro`) with translated content. ⚠️ These are CONTENT pages (text hardcoded per locale), NOT dict-driven — so they do NOT auto-sync across locales; changing a support page means editing the en + de + ja file. Footer SUPPORT links use `localizeUrl` again (en→`/support/faq.html`, de→`/de/…`, ja→`/ja/…`); `SupportPage` breadcrumb "Home" is localized (Home/Startseite/ホーム). 0 mojibake verified across all 12 de/ja pages. Privacy/Terms use formal "governed by the applicable quotation / order confirmation / written agreement" wording (the earlier "template / not legal advice" disclaimer was removed at the user's request — content is still owner/legal-reviewed, just not self-labeled as a draft); Warranty avoids any hardcoded period ("terms vary by product category … confirmed in the applicable quotation/order confirmation/written agreement"). ⚠️ **Header mega-menu still uses 2 business-meeting photos** (`/office-team-discussion.jpeg` + `/business-operations-meeting.jpeg`, as `--submenu-image` in `Header.astro` ~L255/L299) — the user dislikes meeting imagery; NOT changed here (Header is out of scope) — swap to industrial scenes when Header work is approved. ⚠️ **Still placeholder (no page yet):** `/about`, `/certifications`, `/downloads`, `/solutions/electronics-manufacturing`, `/solutions/robotics-automation`. Product/category footer labels (`main.332/333/334/419`) were Title-Cased.
- **Pages**: index, [category], automated-vision-inspection, contact-sales — each × en/de/ja.

- **2026-06-04 support legal hero images:** Privacy Policy and Terms and Conditions no longer use blank/dark-only heroes. They now use dedicated single-purpose generated assets `support-privacy-policy-hero.png` and `support-terms-conditions-hero.png` in both `astro/public/` and `public/`, wired across en/de/ja page files. Keep them text-free (no baked-in readable copy) and do not reuse them for product/menu/commerce imagery.

## 5b. Engineering foundation (scaffold) — added 2026-06-06
A maintainable foundation was added **additively** (new files; nothing existing rewritten **except** `Layout.astro` now imports `SITE_URL` from `config/site.ts`, byte-identical output). Parts are now wired (see the 2026-06-07 pass below); the rest is adopt-incrementally. Full guides in repo-root **`docs/`** (ARCHITECTURE / ROUTES / COMPONENTS / I18N / SEO / CONTENT_GUIDE / DEPLOYMENT / FUTURE_INTEGRATIONS / TROUBLESHOOTING).

**2026-06-07 optimization pass:** ✅ **FAQ** data-driven (`data/faq.ts` `Record<Locale,string>` + `components/support/FaqList.astro`; the 3 faq pages are thin wrappers; rendered content byte-identical). ✅ **Products** consolidated — `ProductPage.astro` + `lib/searchIndex.ts` read the `data/products.ts` accessor (`getCatalog`/`getCategoryGroups`); product pages, sitemap, search index, and product JSON-LD all share one source (deploy-neutral). ✅ **Contact-form status messages** localized en/de/ja (`ContactSales.astro` `STATUS` map → form `data-*` attributes); a full de/ja visible-text scan = **0 residual English**. ✅ **`MAIN_NAV`** corrected to mirror the 6 Header triggers (i18n key + href). ✅ **Header mega-menu PANEL rewrite DONE (2026-06-07)** — the nav (top-level + every panel) is now data-driven from `data/header-navigation.ts`; output verified **byte-structure-identical** across en/de/ja (see the dedicated 2026-06-07 entry below). Footer stays data-driven (`FOOTER_NAV`), untouched. Support legal pages (warranty/shipping/privacy/terms) stay per-locale prose.

**2026-06-07 Header nav data refactor (the deferred high-risk one — now done):** the entire header nav — the 6 top-level triggers AND every mega-menu panel — is data-driven. **New:** `src/types/navigation.ts` (model) + `src/data/header-navigation.ts` (`HEADER_NAV`, the single source) + `src/components/navigation/{HeaderNav,MegaMenu,MegaMenuFeature,MegaMenuSection,MegaMenuLink}.astro` (stable renderers). `Header.astro` keeps only chrome (top strip / brand / search / lang switch / CTA / the inline scrollbar + active-submenu + close-on-click scripts) and calls `<HeaderNav/>`. `MAIN_NAV` is now **derived** from `HEADER_NAV`; `FOOTER_NAV` untouched. **Method (no blind rewrite):** the model was *programmatically extracted* from the old markup with a line-parser (no hand-retyping of 149 links), then the rebuilt header was verified **byte-structure-identical** to the old `public/` across all **81 header pages × en/de/ja** (normalized-whitespace diff = 0; the 9 redirect stubs legitimately have no header), with identical counts (149 `<a>`, 34 panel images, 28 `.mega-link-group`, 7 section labels, 3 `submenu--intro`, 1 bare `<a>`, the literal "FAQ"), and bytes **before** `<div class="top-strip">` / **after** `</header>` byte-identical (body/footer/`<head>` untouched). So the desktop CSS, the `Header.astro`/`Home.astro` inline scripts, and `mobile-navigation.js` (which clones the rendered DOM) all keep working. **To maintain:** edit `header-navigation.ts` ONLY (see `docs/CONTENT_GUIDE.md` + `docs/COMPONENTS.md` "Header navigation"); the rendered DOM is a hard contract — re-run the byte-diff gate (`docs/TROUBLESHOOTING.md` "Verify a header refactor") on any nav change.

- **Scope (hard):** brand site **gyutron.com only**; shop.gyutron.com excluded. **No real backend is connected** — CMS / CRM / Agent / database / e-commerce payment are **mock / placeholder / TODO** (no real services, secrets, or DB).
- **`astro/src/config/`** — `site` (`SITE_URL`/`SITE_NAME` — the single source `Layout.astro` now imports), `seo` (defaults + Organization JSON-LD), `i18n` (RE-EXPORTS `src/i18n` — single source, NOT a duplicate), `routes`, `integrations` (provider switches; default mock/local).
- **`astro/src/types/`** — shared TS types (site/product/solution/support/inquiry/locale/api/agent). **No order/payment/cart/inventory types** by design.
- **`astro/src/data/`** — `navigation`, `company`, `faq`, `solutions`, `support`, `locales` + a typed `products.ts` accessor over the per-locale `products.{en,de,ja}.js` (does NOT merge them). They reference i18n keys.
- **`astro/src/lib/`** — mock adapters behind business functions: `forms` (`submitContactForm`/`validateContactForm`/`normalizeContactPayload` — **now used by the contact page**: `ContactSales.astro`'s submit handler is a bundled `<script>` that calls them instead of an inline `fetch`; still POSTs the existing `/api/contact`), `crm`, `cms`, `agent`, `logger`, `api`. Pattern: page → business fn → adapter (chosen by `config/integrations.ts`). Pages must never call a third-party SDK directly.
- **`astro/src/components/{common,seo,forms}/`** — new presentational components (`SectionTitle`, `CtaSection`, `SeoHead` = upgraded head adding Twitter Card + `og:image` + JSON-LD, `InquiryForm` placeholder). `{layout,navigation,language}/README.md` mark the target grouping; existing `Header`/`Footer`/`LangSwitch` are NOT moved.
- **`astro/.env.example`** — `PUBLIC_SITE_*` + CRM/CMS/ANALYTICS/AGENT providers (mock). **No payment vars.** Real worker secrets (`RESEND_API_KEY`, `CONTACT_FROM_EMAIL`) stay in Cloudflare.

**Run / build / verify**
- `cd astro && npm install` (deps = `astro` only).
- `npm run build` — builds the site; a missing i18n key throws (the gate). Run after every change.
- `npm run verify:header` — **header regression gate** (`scripts/verify-header-equivalence.mjs`). Run after build whenever you touch `Header.astro`, `components/navigation/*`, or `data/header-navigation.ts`: STRICT-asserts the built header (en/de/ja) is whitespace-normalized **equivalent to the deployed `public/`** and the structural/mobile-hook contract holds. Intentional nav change → `npm run verify:header -- --report` (content deltas become INFO; structural contract stays a hard gate), then sync `dist → public`. See `docs/TROUBLESHOOTING.md` "Verify a header / nav change".
- `npm run verify:sitemap` / `verify:routes` / `verify:seo` / `verify:a11y-lite` / `verify:i18n` / `verify:assets` (and `verify:all` = header+sitemap+routes+seo+a11y-lite+i18n) — site-wide gates (all read `astro/dist`, run after build): **sitemap** invariants (every `<url>` has hreflang en/de/ja + x-default, canonical www origin, NO shop, NO redirect stubs, `<loc>` count = paths×locales); **routes** = the `config/routes` core pages exist in en/de/ja AND every Header/Footer internal link resolves to a built page (known footer placeholders are listed, not failed); **seo** = every built page's `<head>` has title/description/robots/canonical (correct locale prefix)/hreflang en/de/ja+x-default/og+twitter/≥1 valid JSON-LD (redirect stubs skipped); **i18n** = heuristic residual-English scan of de/ja pages (REPORT-only — exits 0; product-spec value translation is still deferred, so it flags those by design; `-- --strict` to gate once that's done).
- **CI** (`.github/workflows/verify.yml`): on push/PR to `main`, runs `npm install` + `npm run build` + verify:header/sitemap/routes/seo/a11y-lite as **hard gates** (verify:i18n + verify:assets informational). It **never deploys** — Cloudflare still serves the committed `public/`.
- **No `lint` / `typecheck` scripts exist.** `npm run check` (astro check) needs `@astrojs/check`+`typescript` — NOT installed; don't add it casually. To typecheck the scaffold TS: write a temp tsconfig extending `astro/tsconfigs/strict` that includes `src/{config,types,lib,data}` and run the local `node_modules/.bin/tsc --noEmit -p <it>` (exit 0 = clean), then delete it.

**Dev principles (apply on every wiring step)**
Small steps · build-verify each step · keep it rollback-friendly · don't change existing page URLs · no heavy deps · stay Astro (no Next.js) · keep all future integrations mock/placeholder until truly needed · don't over-engineer (§11).

**Next steps (incremental adoption, low→higher touch)**
1. ✅ **Footer nav** now reads `data/navigation.ts` `FOOTER_NAV` (done 2026-06-06 — `Footer.astro` maps the link columns; build output verified byte-identical, so no `public/` re-render). 2. ✅ **Header nav → data-driven** (done 2026-06-07 — `data/header-navigation.ts` `HEADER_NAV` + `components/navigation/*`; top-level + every mega-menu panel; output byte-structure-identical across en/de/ja, `public/` re-rendered 81 pages, `MAIN_NAV` now derived). 3. ✅ **`Layout.astro` renders `<SeoHead/>`** (done 2026-06-06 — adds `robots`, `og:image`, Twitter Card, Organization JSON-LD via `config/seo.ts` + `utils/seo.ts`; existing tags preserved, `<body>` byte-identical, `public/` rebuilt 81 pages. `MetaRedirect.astro` stays a minimal noindex redirect stub (no og/twitter/JSON-LD) — its canonical now comes from `config/site` (`SITE_URL`) via `utils/seo` `canonicalUrl`, no hardcoded domain (output byte-identical). per-page JSON-LD added 2026-06-06 — see below). 4. i18n residual cleanup. 5. Data-drive content gradually — Products (already data) · Support/FAQ (`data/faq.ts`) · Solutions. ✅ **sitemap** now generated by `src/pages/sitemap.xml.ts` (build-time endpoint, NO `@astrojs/sitemap` dep; en/de/ja + reciprocal hreflang + x-default, www origin, excludes shop + redirect stubs; 81 URLs) and `robots.txt` Sitemap line unified to www. ✅ **structured data**: `utils/structured-data.ts` builds Organization (site-wide) + `BreadcrumbList` (product/solution/support/legal/contact) + `CollectionPage`+`ItemList`/`Product` on category pages — passed via `Layout structuredData` → `SeoHead` (which now accepts an array → one `<script>` per node). **No commerce fields** (no Offer/price/availability/rating/review). Home keeps Organization-only (unchanged).

## 6. HERO — `HeroSlider.astro` + `src/data/heroSlides.ts` + `public/hero-slider.css`
The most-iterated piece. **3 DISTINCT layouts** (field `layout` in `heroSlides.ts`), not one template. **No colored bars** (removed — old AGENTS.md line 14 is stale).
- **① `solutions`** — brand CAPABILITY overview (user: slide 1 must overview *overall capability coverage*, not one narrow solution). Left copy (kicker `main.265`, title `main.350` "Hardware that sees, senses, scans, measures, and acts.", sub `main.351`, CTAs `main.347` + `home.tc.cta`) + right **2×2 industry photo collage** (`/nav-industry-{automotive-ev,electronics-smt,semiconductor,logistics-rfid}.png`, captions `home.tc.{auto,elec,logistics}.label` + `hero.s1.semi`).
- **② `grid`** — FARSET-style product family grid. Left copy (kicker `main.004`, title `hero.s2.title`, sub `main.363`, CTA `main.347`) + right **4×3 grid of 12 transparent SKU images** from `/product-library/transparent/`, ≥1 per major product line.
- **③ `product`** — single-product spotlight (currently `GY-VM900`). Big transparent product image from `/product-library/transparent/` on dark + purple radial glow (`.hsl-prod-visual::before`); eyebrow/title/summary/specs/tags pulled LIVE from `products.{locale}.js`; CTA `main.338`. **Animation: product enters FIRST (~250ms), text staggers after (`PSEQ` in HeroSlider.astro)** — user requirement "先产品后文字".
- **2026-06-04 hero image refresh:** slide 2 and slide 3 must use the new SKU image library under `/product-library/transparent/`, not the older `/product-cutouts/generated/` assets. Slide 2 currently uses 12 maintained SKU PNGs from mobile data capture, vision, sensing, safety/I/O, and metrology; slide 3 spotlights `GY-VM900` from `dimensional-gauges`. Do not revert these to older cutouts because their physical purple accents drifted from the logo purple.
- **2026-06-04 Products mega-menu image refresh:** the Products menu category images for `Rugged PDA Terminals`, `Sensing & I/O`, and `Quality & Test Instruments` were regenerated and deployed as `public/product-intelligent-hardware.png`, `public/product-hero-industrial-sensors-matrix.png`, and `public/product-hero-inspection-instruments-matrix.png`. `Machine Vision Systems` intentionally remains `product-smart-camera-detail.png` because it already combines relevant product + application context. Future category menu images must be directly category-relevant, application-aware where possible, and must not contain baked-in black left-side obstruction/fade panels.
- **2026-06-04 More Products menu panels:** `Robot Workcells`, `Edge Controllers`, and `Accessories` are secondary/non-core product areas, so their Products mega-menu entries use a lightweight `submenu--intro` panel: blank image placeholder + the existing short description only, with no tertiary category list. Fill the placeholder image later only after those product lines are properly developed.
- **2026-06-05 More Products menu image refresh:** `robot-workcells`, `edge-controllers`, and `accessories` are real Astro product categories in `products.{en,de,ja}.js` with 4 SKUs each and nav group `more`; English/German/Japanese pages are generated at `/robot-workcells.html`, `/edge-controllers.html`, `/accessories.html` (+ `/de/`, `/ja/`). SKU assets remain dedicated transparent PNGs under `/product-library/secondary/`; product-page hero images remain `product-hero-robot-workcells-stage.png`, `product-hero-edge-controllers-stage.png`, `product-hero-accessories-stage.png`. Products menu images are still named `product-menu-robot-workcells-stage.png`, `product-menu-edge-controllers-stage.png`, `product-menu-accessories-stage.png`, but were regenerated as bright factory operation/application scenes to match the other product menu panels. These three layers must stay separate. Do not replace More Products menu panels with dark studio/product-display imagery; they should look like bright industrial work under factory lighting, with category-relevant equipment in use.
- **2026-06-05 Robot Workcells SKU material correction:** the four Robot Workcells SKU images (`GY-RC6 Inspect`, `GY-RC12 Tend`, `GY-RC4 Pick`, `GY-RC14 Pack`) were corrected from black/dark graphite robot arms, gantries, platforms, and extrusion structures to white/silver collaborative robot arms, natural silver aluminum profiles, light gray metal platforms/fixtures, and restrained `#4b2e83` accents. The rejected black versions are archived under `asset-workbench/product-images/rejected/robot-workcells-black-2026-06-05/`; corrected candidates live under `asset-workbench/product-images/candidates/robot-workcells-white-metal-2026-06-05-v2/`; live files are `astro/public/product-library/secondary/` + `public/product-library/secondary/`. Future Robot Workcells renders must match the homepage/menu white-silver robot workcell visual language. Do not generate black robot arms, black gantries, or black aluminum extrusion platforms for this family.
- **2026-06-05 Robot Workcells regenerated SKU correction:** the first white/silver correction was rejected because it effectively whitewashed black cameras, LED lights, controllers, I/O boxes, and electronics. The live Robot Workcells SKU files were regenerated from new chroma-key sources, then converted to transparent PNGs. Material rule is now layered: robot arms, aluminum extrusion, platforms, fixtures = white/silver/light gray; machine-vision cameras, lenses, barcode/vision sensors, LED lights, controller modules, I/O boxes, cables, electronics = black/dark graphite; `#4b2e83` only as restrained physical accent trim. Chroma sources live under `asset-workbench/product-images/chroma/robot-workcells-regenerated-2026-06-05/`; final candidates under `asset-workbench/product-images/candidates/robot-workcells-regenerated-2026-06-05/`; rejected whitewashed files under `asset-workbench/product-images/rejected/robot-workcells-whitewashed-2026-06-05/`.
- **2026-06-04 Products menu intro-panel visual fix:** `submenu--intro` image panels are styled only in shared `nav-chrome.css`. Do not apply strong white/gray overlay gradients to these image panels; it washes out the industrial scene and makes the asset look broken. Current rule uses only low-opacity dark/transparent overlays plus mild contrast/saturation. Menu/link clicks now add `body.nav-is-navigating` and immediately clear `.mega-link-group.is-open` to prevent the full Products menu from lingering during page navigation; keep this close-on-click behavior for all desktop mega-menu links.
- Only **2** hero-specific i18n keys exist: `hero.s1.semi`, `hero.s2.title` (each en/de/ja). Everything else reuses existing translated keys + per-locale product data.
- **Mechanism (do NOT regress to WAAPI — it rendered all-black):** slides crossfade via `.hsl-slide.is-active` + CSS opacity/visibility; layers enter via `@keyframes hslIn` (per-layer inline `--fx/--fy/--fs/--delay/--dur`). Inline `<script is:inline>` toggles `.is-active` and autoplays (`cycle = entrance + HOLD 3300ms`); dot click navigates; **focus-pause only — hover does NOT pause** (`CONFIG.pauseOnHover:false`, user requested); `prefers-reduced-motion` respected. Accent purple; 3 square dots.
- **📱 Mobile (≤860px) = FLOW layout, not absolute.** On mobile, slides become `position:relative` + only `.is-active` shown (`display:none` for others) so `.hsl{height:auto}` sizes to the active slide's content → **zero clipping at any phone height**; dots flow below. (A fixed-height absolute-layer hero clipped tall mobile content — that was the bug, fixed in the `hero-slider.css` ≤860 media query.)

## 7. Product image cutouts — and the user's preference
- Tool: **`D:\Codex Data\_cutout.py`** (Python + Pillow + numpy). Source `gy-*.png` are white-background catalog renders. It removes the background by **unmatte-from-white**: connected-background flood-fill from the borders (thresh ~235) → alpha = 1−min(rgb) in that region → un-blend the white `F = (observed − (1−α)) / α`. Result: clean anti-aliased edges, **no white halo**, colors faithful (looks identical when placed back on white, so it stays consistent with the product pages). Run `python _cutout.py --batch` → writes to `public/product-cutouts/` + `astro/public/product-cutouts/`. Verify by compositing on DARK and viewing (transparency can't be judged on a white preview).
- ⚠️ **The user is NOT satisfied with auto-generated cutouts and prefers to own the product imagery** (or use a dedicated image tool). Do not ship questionable cutouts. Prefer asking the user for transparent / dark-background product renders, and keep imagery CONSISTENT across each product line by locking the approved product/camera XYZ direction angles. Canvas ratio, subject scale, side thickness, baseline, and local feature geometry may vary when the model design requires it — see AGENTS.md product-catalog rules except where superseded by the 2026-06-03 direction rule below.
- **2026-06-02 image rule:** for homepage product matrices and any product that will be listed in a catalog category, treat each product as its own maintainable asset. Prefer final transparent PNGs from the start. Do **not** create white-background product renders for production; if a chroma-key background is used as an intermediate, remove it locally and deploy only the final alpha PNG. Avoid one baked 12-product matrix image because the user may swap a single product later.
- **2026-06-04 single-use image rule:** one image file has exactly one visual purpose. Do **not** reuse the same file for a SKU, menu panel, product-page hero, homepage slide, card background, or another slot. If a category needs a menu image and a product-page hero, generate two distinct images. Do **not** generate one broad scene and crop it into multiple SKU files. If reuse/cropping is discovered, mark the assets rejected in `docs/product-image-status.json`, archive the sources, and replace them before deploy.
- **2026-06-03 product accent color rule:** generated product renders must use the official GYUTRON logo purple for physical device accents: `#4b2e83` (same as `--brand-purple` / `gyutron-logo-purple.png`). Do not let image generation drift into blue-purple, pink-purple, neon purple, or the lighter UI accent `#8a63d2`. `#8a63d2` is only for readable UI highlights/glows on dark backgrounds; product buttons, trims, rings, side rails, and brand accent panels should match `#4b2e83`.
- **2026-06-03 direction consistency rule:** generated product renders in the same product family must share the same 3D direction angles: lock the product/camera orientation as a consistent X/Y/Z rotation-angle template. Do **not** turn this into hard requirements about front-face ratio, visible side thickness, top scanner shape, exact baseline, or product proportions; those may vary by model. The hard gate is that the same family should not randomly rotate, flip, lean, or change camera-side angle between SKUs. Do **not** batch-generate a product family until one XYZ direction-angle template is approved. If direction angle differs, mark the asset rejected in `docs/product-image-status.json`, archive it under `asset-workbench/product-images/rejected/`, and keep it out of `astro/public/product-library/transparent/` and `public/product-library/transparent/`.
- **2026-06-05 machine-vision connector rule:** machine-vision cameras, fixed code readers, 3D vision modules, and vision lights must not show power/data/I/O/M12 connectors on the visible side face in front or front-right 3/4 product renders. Realistic catalog renders should put connectors on the rear/back face; if that rear face is not visible, the connector should be hidden. The approved fix is to use the original product image as a visual template and **regenerate a complete product render** with no visible side connector while preserving the original direction, lens/lighting layout, proportions, and material language. Do **not** crop, erase, mask, or locally cut off a connector from an existing PNG: this leaves broken product geometry and is rejected.
- **2026-06-06 machine-vision no-side-IO continuation:** `GY-V280 AI`, `GY-V380 Pro`, `GY-V3D90`, `GY-V3D150`, `GY-VL2048`, and `GY-VRG200` were regenerated through the built-in image tool (not API/`OPENAI_API_KEY`) as complete product renders with clean visible side/end heat-fin housings and no visible power/data/I/O/M12/socket/cable connector. Final transparent assets are live in `astro/public/product-library/transparent/` and `public/product-library/transparent/`; chroma/candidate/rejected archives live under `asset-workbench/product-images/{chroma,candidates,rejected}/machine-vision-*-2026-06-06/`; QA/status recorded in `docs/product-image-status.json` and library rebuilt. No-side-IO follow-up for Code Reading and Vision Lighting is also complete as of 2026-06-06. Continue with the same built-in/chroma workflow unless the user explicitly authorizes another path.
- **2026-06-03 direction QA correction:** the user flagged `GY-LD40` and `GY-SAFE90` as reversed after the sensing batch shipped; both were horizontally corrected and marked `corrected-wired` in `docs/product-image-status.json`. For every future batch, compare each new SKU against at least one same-family reference contact sheet before commit. If two products expose opposite left/right side faces, opposite bracket side, or mirrored front/side relationship, treat it as a failed direction gate even when the individual render looks good.
- **Product image library (2026-06-03):** before generating, replacing, or wiring product images, read `docs/product-image-library.json` / `.md` and `docs/product-image-generation-plan.md`. Rebuild the library with `node tools/build_product_image_library.mjs` after any product data/status change; per-model QA/status lives in `docs/product-image-status.json`. Final reusable transparent PNGs belong in `astro/public/product-library/transparent/` (and build/sync to `public/product-library/transparent/`). Chroma-key intermediates and source archives belong under `asset-workbench/product-images/`, not as live site assets. The library maps all 93 current product models to category, current image, target transparent image, visual family, direction, prompt seed, and status for Codex/Claude lookup. Run `python tools/harmonize_product_purple.py --input <final.png> --out <final.png>` when generated product accents drift from logo purple `#4b2e83`.
- **2026-06-03 GY-A50 live image:** `asset-workbench/product-images/candidates/gy-a50-touch-direction-template-transparent.png` was generated from the user's green-background PDA reference, harmonized to `#4b2e83`, copied to `astro/public/product-library/transparent/gy-a50.png`, and wired to GY-A50 in `products.en/de/ja.js`. It is the first live test of the XYZ direction-angle rule; previous rejected direction renders remain archived under `asset-workbench/product-images/rejected/`.
- **2026-06-03 mobile data capture image batch:** all products in these four tertiary categories are generated, converted to transparent PNGs, harmonized to `#4b2e83`, and wired in `products.en/de/ja.js`: `android-pda` (8), `rfid-handhelds` (5), `barcode-scanners` (8), `request-specification` (3). Final assets live in `astro/public/product-library/transparent/` and `public/product-library/transparent/`; chroma/source archives live in `asset-workbench/product-images/chroma/` and `asset-workbench/product-images/source/`; preview contact sheets live in `asset-workbench/product-images/candidates/*-contact-sheet.png`.
- **2026-06-05 Android PDA / Edge Controller pruning:** `GY-A55 Pro` and `GY-A92K KeyTouch` were removed from `android-pda` because they were too close to adjacent PDA models in both parameter coverage and visual form. The live Android PDA line is now 6 SKUs: `GY-A50`, `GY-A60 Max`, `GY-A62K`, `GY-A70 Cold`, `GY-A80 Ultra`, `GY-A90 Touch`. `GY-EC500 Vision` was removed from `edge-controllers`; the live Edge Controller line is now `GY-EC200 DIN`, `GY-EC800 Motion`, `GY-EC900 Gateway`. Current PDA images remain usable but visually same-family; when image generation capacity is available, prioritize distinct replacement renders for `GY-A60 Max`, `GY-A70 Cold`, and `GY-A90 Touch` rather than restoring the deleted near-duplicate SKUs.
- **2026-06-03 sensing image batch:** all products in these three tertiary categories are generated, converted to transparent PNGs, harmonized to `#4b2e83`, and wired in `products.en/de/ja.js`: `proximity-sensors` (6), `laser-measurement` (3), `environmental-sensing` (4). Final assets live in `astro/public/product-library/transparent/` and `public/product-library/transparent/`; chroma/source archives live in `asset-workbench/product-images/chroma/` and `asset-workbench/product-images/source/`; preview contact sheets live in `asset-workbench/product-images/candidates/{proximity-sensors,laser-measurement,environmental-sensing}-contact-sheet.png`.
- **2026-06-03 machine vision image batch:** all products in these four tertiary categories are generated, converted to transparent PNGs, harmonized to `#4b2e83`, direction-checked by contact sheet, and wired in `products.en/de/ja.js`: `area-scan-cameras` (10), `smart-vision-sensors` (10), `code-reading-cameras` (9), `vision-lighting` (5). Final assets live in `astro/public/product-library/transparent/` and `public/product-library/transparent/`; chroma/source archives live in `asset-workbench/product-images/chroma/` and `asset-workbench/product-images/source/`; preview contact sheets live in `asset-workbench/product-images/candidates/{area-scan-cameras,smart-vision-sensors,code-reading-cameras,vision-lighting}-contact-sheet.png`.
- **2026-06-06 Code Reading & Verification regeneration:** all 9 code-reading products (`GY-CR120`, `GY-CR390`, `GY-CR520 DPM`, `GY-CR720 Conveyor`, `GY-S300 DPM`, `GY-CV100`, `GY-CV220 Inline`, `GY-RT800`, `GY-CR900 Matrix`) were regenerated through the built-in image tool (not API/`OPENAI_API_KEY`) as complete product renders with no visible side/rear cable, M12, Ethernet, power, or I/O connector. The approved family direction is left-facing: optical face/lens on the left/front side, body depth receding right/back; do not batch this family with prompts that can flip the optical face to the right. A first wrong-direction attempt is archived under `asset-workbench/product-images/rejected/code-reading-wrong-direction-2026-06-06/`; final chroma/candidates are under `asset-workbench/product-images/{chroma,candidates}/code-reading-left-facing-2026-06-06/`, final transparent assets are live in both `astro/public/product-library/transparent/` and `public/product-library/transparent/`, and the QA contact sheet is `asset-workbench/product-images/candidates/code-reading-left-facing-2026-06-06-transparent-contact-sheet.png`. Future code-reading/verifier renders must be plausible real industrial products, not fantasy gantries or white-box chambers; check direction + no-side-IO on a contact sheet before commit.
- **2026-06-06 Vision Lighting regeneration:** all 5 vision-lighting products (`GY-LR70`, `GY-LB220`, `GY-LDome120`, `GY-LBL150`, `GY-LL300`) were regenerated through the built-in image tool (not API/`OPENAI_API_KEY`) as complete product renders with sealed housings/end caps and no visible side/rear cable, M12, Ethernet, power, or I/O connector. Final chroma/candidates are under `asset-workbench/product-images/{chroma,candidates}/vision-lighting-no-side-io-2026-06-06/`, old side-connector assets are archived under `asset-workbench/product-images/rejected/vision-lighting-side-io-2026-06-06/`, final transparent assets are live in both `astro/public/product-library/transparent/` and `public/product-library/transparent/`, and the QA contact sheet is `asset-workbench/product-images/candidates/vision-lighting-no-side-io-2026-06-06-transparent-contact-sheet.png`. Future vision-lighting renders should keep the light type distinct (ring / bar / dome / backlight / line light), avoid visible connectors, and verify on a contact sheet before commit.
- **2026-06-04 quality/test instruments image batch:** the final four tertiary categories are generated, converted to transparent PNGs, harmonized to `#4b2e83`, direction-checked by contact sheet, and wired in `products.en/de/ja.js`: `dimensional-gauges` (4), `surface-inspection` (3), `portable-testers` (3), `calibration-tools` (3). Final assets live in `astro/public/product-library/transparent/` and `public/product-library/transparent/`; chroma/source archives live in `asset-workbench/product-images/chroma/` and `asset-workbench/product-images/source/`; preview contact sheets live in `asset-workbench/product-images/candidates/{dimensional-gauges,surface-inspection,portable-testers,calibration-tools}-contact-sheet.png`. First-pass renders that put readable text on instrument screens/dials were not wired; future product images should keep screens abstract/non-readable to preserve i18n readiness.
- **Image brief / prompt the user can hand to an image tool or designer** (also given to them directly):
  > Produce a clean **transparent-PNG cutout** of this GYUTRON industrial product render (rugged PDA / scan gun / machine-vision camera / sensor / instrument), to float on a near-black hero.
  > 1. Remove the white/light background AND the contact shadow → full alpha transparency.
  > 2. **Edges must be clean, anti-aliased, with NO white/light halo or fringe** — the white must be *decontaminated* from semi-transparent edge pixels, not merely made transparent. (This is the #1 failure to avoid.)
  > 3. **Preserve the product EXACTLY** — identical colors, materials, screen, buttons, proportions, angle and markings as the original; when placed back on white it must look identical to the catalog render. Do NOT re-render, stylize, beautify, or AI-imagine the device.
  > 4. Device accent color must match the GYUTRON logo purple `#4b2e83` exactly; do not use blue-purple, pink-purple, neon purple, or the lighter UI accent `#8a63d2`.
  > 5. Keep original (or higher) resolution; output PNG-24 with alpha, trimmed to the product's bounding box.
  > 6. Don't bake in rim lighting — that's added in CSS.
  > Within one product line, keep the approved product/camera XYZ direction angles consistent. Canvas ratio, subject scale, local feature geometry, side thickness, handle/antenna form, and baseline may vary when the model design requires it, but do not randomly rotate, flip, or change the 3D camera angle between SKUs.

## 8. Gotchas
- **Verify animated UI by its NATURAL settled state, not force-settled** — force-settling once masked an all-black hero. Always visually verify desktop + mobile before shipping.
- **Headless preview recipe** (no connected Chrome): build first → write `D:\Codex Data\.claude\launch.json` = `{version:"0.0.1",configurations:[{name:"gyutron-astro-preview",runtimeExecutable:"cmd",runtimeArgs:["/c","cd /d D:\\Codex\\workspaces\\2026-05-17\\files-mentioned-by-the-user-gyutron\\gyutron-website-repo\\astro && npx astro preview --port 4321"],port:4321,autoPort:false}]}` → `preview_start` → `preview_resize` 1440×900 / 390×844 → screenshot / inspect / console(level=error) → navigate via eval `location.assign('/de/index.html')` → `preview_stop` + delete launch.json. Free port 4321 first. To screenshot a *settled* slide, set the slide's OWN `opacity/visibility/transition` (not just the layers), else the 750ms crossfade leaves the shot on the previous slide. If `preview_screenshot` keeps timing out (infra) but `eval` works, verify via `getBoundingClientRect`/computed styles + offline composites instead.
- `PYTHONUTF8=1 PYTHONIOENCODING=utf-8` for any Python touching de/ja text (console is GBK). For precise JSON key edits, text-replace anchored on `"key": "value"` (assert count==1) to avoid reformatting whole dicts.
- The shell/bash tool resets cwd each call — use absolute paths or `git -C <repo>`.
- **Mega-menu submenu is JS-driven (2026-06-03→06, HEAD `89d3f8c`).** `Header.astro`'s inline `<script>` toggles `.is-open` on the active `.mega-link-group` (default = first group on the nav-item's `pointerenter`, switch on group hover, **sticky** while the pointer is inside that group's submenu; adapts to any group count via `querySelectorAll('.mega-link-group')`). It is **NOT cleared on leave** — the submenu stays put while the menu closes so the overview montage never flashes through; re-opening resets to the first group. The 3 per-page CSS files (`global`/`product-page`/`contact-page`) reveal `.submenu` **only** on `.is-open` via an appended `!important` block; `nav-chrome.css` forces `.submenu{transition:none}` AND hides `.mega-menu::after{display:none}` (the menu-level montage is vestigial now that a submenu is always shown). **Do NOT** (a) add `:hover`/`:has()` rules that show a submenu, (b) re-add a clear-on-leave, or (c) un-hide `.mega-menu::after` — each one regresses a fixed bug: blank panel on tab-hover / revert-to-first when entering the submenu / overview-montage flash on close.
- **Nav hover zones are seamless (2026-06-06, `nav-chrome.css`).** `.nav` flex `gap` is `0 !important` and the inter-title spacing is moved into transparent `.nav-item` padding (4px/side base; 1px/side in the `1181–1380px` tier) so adjacent nav-item hover boxes touch edge-to-edge — moving the pointer between titles never crosses a dead strip that would close+reopen the mega-menu (flicker). **Do NOT re-introduce a `.nav { gap }` or drop the `.nav-item` padding** unless the hover boxes stay contiguous. (`.nav-item` is `position:static`, so its padding never shifts the centered mega-menu.)
- **Home mega-menu closes with ZERO delay (2026-06-06, `Home.astro`).** The HOME page has its OWN desktop-nav script (`openItem`/`closeItem`/`setActiveGroup`, separate from `Header.astro`'s IIFEs) that opens via `.nav-item.is-open`. `closeItem` is now **synchronous** (it had a 520ms `closeDelay` that made the menu linger ~½s after the pointer left) and the redundant `menu.pointerleave→closeItem` was removed (so menu↔trigger moves don't blank/close — `item.pointerleave` already covers a true leave, since the nav-item's hover area includes the mega-menu child). **Do NOT re-add a `closeDelay` or a `menu.pointerleave→closeItem`.** Other pages already close instantly via pure CSS `:hover`.
- **A submenu is a pointer target ONLY while its menu is open (2026-06-06, `nav-chrome.css`).** The leave-flash fix keeps `.mega-link-group.is-open` after the menu closes, and all mega-menus are centered/overlapping, so giving an active `.submenu` an unconditional `pointer-events:auto` left an INVISIBLE clickable box (~628×464) at the page centre with the menu shut — the pointer crossing it re-opened the menu (popped with no nav hover) or jumped to another tab. pointer-events is therefore gated: `.mega-menu .mega-link-group.is-open .submenu{pointer-events:none!important}` by default, `auto` only under `.nav-item:hover/:focus-within/.is-open`. **Never give a hidden/overlapping `.submenu` an unconditional `pointer-events:auto`.**

## 9. Residual / TODO
- Residual English: `verify:i18n` reports **0 suspects** on rendered de/ja pages. Phase 6 (2026-06-07) corrected the earlier "= 0" claim by fixing product-DATA residuals that bypass the i18n dicts in `products.{de,ja}.js`: the rendered `calibration-tools` `panelText` (ja) + non-rendered `panelMetric`/`sectionIntro` (ja+de) — see docs/I18N.md "Residual-English status". **Still TODO: native language QA of those translations** + **DEFERRED: product SPEC translation** (~85 labels + values, de/ja still English in product cards) — user will supply a glossary or approve MT; final content pass.
- **➡️ Current TODO list lives in the "🤝 CODEX HANDOFF" block at the top of this file.** Done since: sitemap.xml (build-time endpoint) ✅, per-page Twitter tags ✅ (SeoHead), the dead `[data-hero-carousel]` inline script in `Home.astro` ✅ removed in the 2026-06-07 homepage rebuild. Still open: CSS dedup (chrome duplicated across the per-page CSS); retire legacy root-level pages + dead `product-data.js`/`product-catalog.js`.

## 10. Reconciliation with `AGENTS.md` (older; partly stale)
`AGENTS.md` still has many **VALID** rules — navigation structure, brand/logo (`gyutron-logo-purple.png`), responsive breakpoints (1440/1024/768/430/390), store/shop conventions, product-catalog consistency, micro-interactions, Cloudflare routing. Keep following those. **SUPERSEDED items:**
- **Lines 17–18 (run `npm run i18n:build` / `tools/generate_localized_site.py` after edits)** → OBSOLETE & DANGEROUS. That legacy generator clobbers the Astro site. de/ja are built by Astro from per-locale data now. Do NOT run it.
- **Line 14 (hero = colored bars / old carousel structure)** → SUPERSEDED by §6 (3 Astro layouts, no colored bars).
- **Line 16 (de is a homepage-only sample; most subpages still English)** → SUPERSEDED — all 21 pages are localized en/de/ja via Astro.
- **`tools/update_navigation.py` / editing "root HTML pages"** → those root pages are legacy; edit `astro/src/**` (shared chrome = Header/Footer components) instead.

---

## 11. Long-term architecture principles & constraints (apply on EVERY change)
> User directive 2026-06-03. The site is early but must grow into a long-extensible **industrial-tech brand site**: brand site → multilingual → product/solution library → industry-content hub → inquiry-conversion funnel → eventually backend/CRM/product-DB/CMS. **Priority right now = a clean, stable, maintainable, i18n-ready, SEO-friendly, low-maintenance Astro architecture — NOT piling on features.** These are DIRECTIONAL: apply on every change, migrate toward them **INCREMENTALLY** (never a big-bang refactor); if you find the structure messy, **tell the user first + propose SMALL steps.**

**Brand:** GYUTRON / 具创智能 — B2B industrial tech (工业自动化, 机器视觉, 智能机器人, AGV/AMR, 工业相机, 传感器, 自动化零部件); China supply chain → global; overseas B2B. NOT a plain display page, NOT pure e-commerce.

**9 hard principles**
1. **Componentize, never copy-paste.** Product card / industry case / CTA / header / footer / nav recur across home + product + industry + solution pages and all locales → build reusable components from the start.
2. **Data-drive content** → `src/data/*`, not hardcoded deep in components (so text/image/link edits stay easy).
3. **Astro file layout (gradual target):** pages→`src/pages`, components→`src/components` (group `common/ home/ product/ solution/`), layouts→`src/layouts`, data→`src/data`, styles→`src/styles`, static images→`public/images`. No mega-HTML files; no heavy lib/framework for a simple feature.
4. **Static-first.** Prefer Astro static generation; don't turn statically-renderable content into pure client-side JS render. JS only for necessary interactions.
5. **i18n-ready — don't block it.** Don't scatter text deep in components; keep content data-driven; URL structure must allow future locales; img `alt` + meta title/description translatable; Header/Footer/CTA/category names language-switchable. (Don't add a heavy i18n lib unless truly needed.) Future locales contemplated: en/zh/ja/de/es.
6. **SEO.** Exactly one `<h1>` per page; sane h1/h2/h3 hierarchy; unique `<title>` + meta description per page; img `alt`; clean readable URLs; important content output in HTML (not JS-only); home/product/solution/industry pages all indexable.
7. **Performance (global audience: CN mainland, SEA, EU, US).** Compress images; optimize above-the-fold first; no gratuitous big JS libs; CSS animation over heavy animation libs; avoid CLS; smooth mobile; keep the homepage light.
8. **Industrial-tech visual** (工业 / 专业 / 克制 / 硬核 / 国际化 / B2B / 科技但不花哨). Reference QUALITY (never copy assets/copy/logo): Keyence, Cognex, Basler, LUCID Vision Labs, ABB Robotics, OMRON, SICK, ifm. AVOID: 电商大促风 / 小红书风 / 过度霓虹·粒子科技风 / 普通 SaaS 模板风 / 国内低端工业站风. (= existing §2 thinklucid rule + brand purple.)
9. **Don't over-engineer.** User is NOT a programmer; keep the project understandable + AI-maintainable. Avoid needless abstraction, heavy toolchains, unnecessary state management, premature backend/microservices, over-complex TS types, refactor-for-show. **Test: genuinely improves future maintenance → do it; just looks advanced + adds complexity → don't.**

**Future content columns (anticipate, don't build one-off-for-today):** Home · Products · Solutions · Industries · Resources · About · Contact.
- **Products:** Machine Vision · Industrial Cameras · Lenses · Lighting · 3D Vision · Robotics · AGV/AMR · Sensors · Automation Components.
- **Solutions:** Vision Inspection · Vision-Guided Robotics · Warehouse Automation · Factory Automation · Industrial AI · Cross-border Sourcing.
- **Industries:** Automotive · Electronics · Battery · Food & Packaging · Logistics · Metal Processing · Medical Devices · Agriculture.
- **Resources:** Blog · Application Notes · Case Studies · Technical Guides · Downloads · FAQ.

**Backend / forms:** stay an Astro static site for now; **no backend unless explicitly needed** (user system, auth, complex product filtering, CMS, payments, customer-data, CRM, form automation, product DB). Future options when needed: Supabase / PostgreSQL / Airtable / Sanity / Strapi / Directus / HubSpot / custom API — never added "to look advanced." **Inquiry forms** (Contact / Inquiry / Request Quote / Download Datasheet): clear fields, NOT fake front-end-only, ONE reusable component, leave hooks for email/CRM/DB/webhook, carry per-product source + trackable lead-source. Future fields: Name, Company, Email, Phone/WhatsApp, Country/Region, Industry, Interested Product, Project Description, Expected Quantity, Application Scenario. (Contact form already posts `/api/contact` → `contact-handler.mjs`.)

**✅ Pre-change checklist (run before EVERY edit):** 1) which component does this belong in? 2) should this content live in `src/data`? 3) reused later? 4) multilingual impact? 5) SEO impact? 6) mobile impact? 7) slows the homepage? 8) fits the industrial-brand visual? 9) harder to maintain? — **If existing structure is messy: tell the user first + propose SMALL incremental refactors, never a big uncontrolled rewrite.**

**📤 Report after EVERY change:** 1) files changed; 2) components added; 3) where data is maintained; 4) where image assets live; 5) mobile impact; 6) SEO impact; 7) `npm run build` passes?; 8) how to extend it next.

**Ideal vs CURRENT state (gradual; several deliberate — prefer ideal for NEW code, migrate existing only in small ANNOUNCED steps):**
- **Layouts:** `Layout.astro` already in `src/layouts/` ✓.
- **Components:** currently FLAT in `src/components/`; target = group into `common/ home/ product/ solution/`.
- **Styles:** CSS currently in `astro/public/*.css` (loaded via `pageCss`), not `src/styles/` — legacy-extracted; low-priority gradual target.
- **Data:** products in per-locale `products.{en,de,ja}.js` (DELIBERATE — drives build-time per-locale pages + `i18n-audit.py`; keep). `heroSlides.ts` exists; `navigation/solutions/industries/site` not yet extracted to `src/data` — extract when next touched.
- **URLs/i18n:** `/` (en) + `/de/` + `/ja/`, `build.format:'preserve'` byte-matching legacy. **Do NOT blindly switch to `/en/…`** — breaks indexed URLs / needs redirects.
- **Images:** product imagery at `public/product-images/` + cutouts `public/product-cutouts/` (user owns product imagery himself).
