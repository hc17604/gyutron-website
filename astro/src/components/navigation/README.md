# components/navigation/

Target home for navigation pieces (mega-menu, breadcrumbs). The nav data model lives in
`src/data/navigation.ts`; the current markup is still in `Header.astro` / `Footer.astro` and will
be rewired here incrementally. See docs/COMPONENTS.md.
