# components/layout/

Target home for layout chrome — `Header.astro`, `Footer.astro` (and `Layout.astro` lives in
`src/layouts/`). These are **not moved yet**: relocating a component changes its import path in
every page, so it is done one at a time with a build + visual check. See docs/COMPONENTS.md.
