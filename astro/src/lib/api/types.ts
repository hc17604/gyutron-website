/** API layer types — re-exported from the shared types so lib code has one import site. */
export type { ApiResult, ApiError, ApiRequestOptions } from '../../types/api';
export { ok, err } from '../../types/api';
